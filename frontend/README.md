# 부동산 실거래가 React 앱

스크린샷 디자인을 참고하여 제작한 모던한 부동산 실거래가 비교 React 앱입니다.

## 주요 기능

- ✨ 모던하고 깔끔한 UI/UX
- 📱 모바일 최적화 (반응형 디자인)
- 🏠 부동산 매물 카드 형식 표시
- 🔍 검색 및 필터 기능
- 📊 실시간 가격 변동 표시
- 📈 시세 통계 정보
- 💙 관심 매물 저장 기능

## 디자인 특징

- 카드 기반 레이아웃
- 그라데이션 배경과 섹션
- 부드러운 애니메이션
- 직관적인 하단 네비게이션
- 스크린샷과 유사한 모던한 스타일

## 설치 및 실행

### 1. 의존성 설치

```bash
cd frontend
npm install
```

### 2. 개발 서버 실행

```bash
npm start
```

앱이 [http://localhost:3000](http://localhost:3000)에서 실행됩니다.

### 3. 프로덕션 빌드

```bash
npm run build
```

## 프로젝트 구조

```
frontend/
├── public/
│   └── index.html
├── src/
│   ├── components/
│   │   ├── PropertyCard.js       # 매물 카드 컴포넌트
│   │   ├── PropertyCard.css
│   │   ├── NavigationTabs.js     # 상단 탭 네비게이션
│   │   ├── NavigationTabs.css
│   │   ├── SearchBar.js          # 검색바
│   │   ├── SearchBar.css
│   │   ├── FilterSection.js      # 필터 버튼
│   │   └── FilterSection.css
│   ├── App.js                     # 메인 앱 컴포넌트
│   ├── App.css                    # 메인 스타일
│   ├── index.js                   # 앱 진입점
│   └── index.css                  # 전역 스타일
└── package.json

```

## 컴포넌트 설명

### PropertyCard
- 부동산 매물 정보를 카드 형식으로 표시
- 이미지, 가격, 위치, 평수 등의 정보 포함
- 호버 효과와 찜 버튼 기능

### NavigationTabs
- 매물 타입별 탭 (전체, 아파트, 오피스텔 등)
- 활성 탭 시각적 표시

### SearchBar
- 지역/단지명 검색 기능
- 자동완성 지원 준비

### FilterSection
- 거래 유형 필터 (매매, 전세, 월세 등)
- 추가 필터 옵션 버튼

## 스타일 가이드

### 색상
- Primary: `#667eea` - `#764ba2` (그라데이션)
- Text: `#222` (제목), `#666` (일반), `#999` (보조)
- Background: `#ffffff` (카드), `#f8f9fa` (배경)
- Success: `#52c41a`
- Error: `#ff4d4f`
- Info: `#1890ff`

### 타이포그래피
- 제목: 18px, font-weight: 600
- 본문: 14-15px, font-weight: 400-500
- 보조: 12-13px

### 간격
- 섹션 간격: 24-32px
- 카드 간격: 12px
- 내부 패딩: 12-20px

## Backend 연동

Flask backend와 연동하려면:

1. Backend 서버 실행 (포트 5000)
2. `src/App.js`에서 API 호출 추가
3. axios를 사용하여 데이터 가져오기

예시:
```javascript
import axios from 'axios';

const fetchProperties = async () => {
  const response = await axios.get('http://localhost:5000/api/search-complex?keyword=헬리오');
  setProperties(response.data);
};
```

## 향후 개선 사항

- [ ] Backend API 완전 연동
- [ ] 상세 페이지 구현
- [ ] 차트 라이브러리 통합
- [ ] 사용자 인증 기능
- [ ] 관심 매물 저장 (로컬스토리지)
- [ ] 무한 스크롤
- [ ] PWA 지원

## 라이선스

MIT
